export interface IRequestOptions {
    method: String;
    headers: Headers;
    redirect: String;
}
export interface ICookieFormat {
    display_value: string;
    card_image: string;
    premiere: string;
}